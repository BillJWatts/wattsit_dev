[project]
name = "wattsit-dev"
version = "0.1.0"
description = "Personal website for Bill Watts"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[tool.fastapi]
entrypoint = "src.main:app"
